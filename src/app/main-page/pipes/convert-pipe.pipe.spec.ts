import { ConvertPipePipe } from './convert-pipe.pipe';

describe('ConvertPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertPipePipe();
    expect(pipe).toBeTruthy();
  });
});
